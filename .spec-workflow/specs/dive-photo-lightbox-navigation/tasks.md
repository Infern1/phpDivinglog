# Tasks Document

- [x] 1. Enhance the shared lightbox component with grouping and navigation in public/assets/js/lightbox.js
  - File: public/assets/js/lightbox.js (rewrite in place)
  - Build the dialog markup with a toolbar (counter + close), a stage (prev button, image, next button), and a caption line, per the design's Component 2 structure.
  - Implement `resolveGroup(anchor)` using the design's precedence: (1) nearest `[data-lightbox-group]` ancestor → anchors sharing that value; (2) nearest `ul.gallery-grid` ancestor → anchors in that list; (3) standalone single-image group.
  - Implement `open(anchor)` (resolve group, set index to clicked anchor, render, `showModal()`), `render(index)` (set image src/alt, caption, counter "N / M", show/hide nav + counter when the group has one image or fewer), and `next()`/`prev()` with wrap-around.
  - Add keydown handling for ArrowRight (next) and ArrowLeft (prev); keep native `<dialog>` Escape-to-close and focus trapping. Wire prev/next button clicks.
  - Keep the single delegated `document` click listener; guard all DOM access (optional chaining) so missing markup degrades to single-image behavior without throwing.
  - Purpose: Deliver in-lightbox next/previous, keyboard nav, wrap-around, and position indicator scoped per gallery group.
  - _Leverage: existing public/assets/js/lightbox.js (delegated listener + native `<dialog>`), a[data-lightbox] trigger convention, .gallery-grid list wrapper_
  - _Requirements: 1, 2, 3, 4, 5_
  - _Prompt: Implement the task for spec dive-photo-lightbox-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Front-end Developer specializing in dependency-free vanilla JavaScript and the native dialog API | Task: Rewrite public/assets/js/lightbox.js into a grouped gallery browser implementing requirements 1-5 — group resolution (data-lightbox-group → ul.gallery-grid → standalone), next/prev with wrap-around, ArrowLeft/ArrowRight keyboard nav, an "N / M" counter, caption/alt updates, and show/hide of controls for single-image groups — reusing the existing delegated click listener and native dialog element (retain Escape-close and focus trapping) | Restrictions: No third-party libraries or build step; keep a single delegated document listener (no per-thumbnail handlers); treat all DOM-sourced values as data (no unescaped HTML injection); guard every DOM access so missing/partial markup falls back to single-image behavior without errors; do not change any PHP, templates, or data | Success: Opening a multi-image gallery shows working prev/next + counter confined to that gallery; wrap-around works at both ends; arrow keys and Escape work; single-image/standalone triggers hide the nav and counter; no console errors; existing single-image pages still behave correctly. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 2. Style the lightbox navigation controls in public/assets/css/custom.css
  - File: public/assets/css/custom.css (extend the existing .lightbox-dialog block, ~lines 676-699)
  - Add rules for `.lightbox-toolbar`, `.lightbox-stage`, `.lightbox-nav` (`.lightbox-prev`/`.lightbox-next`), `.lightbox-counter`, and `.lightbox-caption`, plus a hidden state (e.g. `.lightbox-nav[hidden] { display: none; }`).
  - Position prev/next as large, theme-aware touch targets flanking the image; reuse the existing theme custom properties (`--surface-container-highest`, `--on-surface`) so light/dark both work.
  - Purpose: Present accessible, on-theme navigation controls, counter, and caption consistent with Material Design 3 styling.
  - _Leverage: existing .lightbox-dialog rules and theme CSS custom properties in public/assets/css/custom.css_
  - _Requirements: 1.1, 4.1, Non-Functional Usability_
  - _Prompt: Implement the task for spec dive-photo-lightbox-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: UI Developer with expertise in CSS and Material Design 3 (Beer CSS) theming | Task: Extend public/assets/css/custom.css with styles for the lightbox toolbar, stage, prev/next nav buttons, counter, and caption (class names per the approved design), reusing the existing .lightbox-dialog rules and theme custom properties so controls render correctly in both light and dark themes and include a hidden state for single-image groups | Restrictions: Do not introduce new dependencies or override unrelated styles; reuse existing theme variables rather than hard-coded colors; keep controls as adequately sized, accessible touch targets | Success: Prev/next buttons flank the image as visible, well-sized, theme-aware controls; counter and caption are legible in both themes; single-image groups hide the nav cleanly; no regressions to the existing lightbox appearance. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 3. Add the explicit grouping attribute to the dive detail gallery in templates/dive_detail.html.twig
  - File: templates/dive_detail.html.twig (the `<ul class="gallery-grid">` at ~line 114)
  - Add `data-lightbox-group="dive-pictures"` to the gallery `<ul>` to document the canonical grouping convention on the primary page (JS already falls back to gallery-grid for the others).
  - Purpose: Make the gallery-grouping contract explicit and self-documenting on the dive detail page.
  - _Leverage: existing gallery markup and a[data-lightbox] anchors in templates/dive_detail.html.twig_
  - _Requirements: 5.1, 5.3, Non-Functional Code Architecture (data-attribute contract)_
  - _Prompt: Implement the task for spec dive-photo-lightbox-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Twig Template Developer | Task: Add data-lightbox-group="dive-pictures" to the Pictures gallery `ul.gallery-grid` in templates/dive_detail.html.twig to declare the explicit grouping convention per requirement 5, leaving the inner anchors/images unchanged | Restrictions: Change only the single ul attribute; do not alter loop logic, other markup, or other templates; keep output auto-escaped | Success: The dive detail gallery ul carries data-lightbox-group="dive-pictures", the page renders unchanged visually, and lightbox navigation still resolves the dive's photos as one group. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 4. Assert gallery markup and lightbox script in the dive detail HTTP smoke test
  - Files: tests/ (locate the existing dive-detail HTTP smoke test; extend it — do not create a new framework)
  - Add assertions that the rendered dive detail page contains the gallery markup (`data-lightbox-group="dive-pictures"` and/or `class="gallery-grid"`) and the `/assets/js/lightbox.js` script tag, guarding against template regressions.
  - Purpose: Provide automated protection that the template contract stays intact.
  - _Leverage: existing PHPUnit HTTP smoke tests and fixtures under tests/_
  - _Requirements: 5.1, Non-Functional Reliability_
  - _Prompt: Implement the task for spec dive-photo-lightbox-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: QA Engineer with expertise in PHPUnit HTTP smoke testing | Task: Extend the existing dive-detail HTTP smoke test under tests/ to assert the rendered page includes the gallery markup (data-lightbox-group="dive-pictures" and/or gallery-grid) and the /assets/js/lightbox.js script tag, per requirement 5 and the reliability NFR, reusing existing test bootstrapping and fixtures | Restrictions: Do not add a JS test framework or new tooling; extend the existing PHP test rather than creating parallel infrastructure; keep tests isolated and fixture-backed (pdo_sqlite) | Success: The smoke test asserts the gallery attribute/class and the lightbox script tag are present, and passes under `composer test`. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 5. Verify the full gate and manually validate acceptance criteria
  - Files: none (verification task)
  - Run `composer test && composer stan && composer cs` and ensure it passes.
  - Manually verify in a browser against the acceptance criteria: dive detail multi-photo prev/next + counter + wrap-around + arrow keys + Escape; single photo hides nav; dive-site map opens standalone while its photo gallery navigates within itself; stats/cert scans navigate only within a single certification's front/back pair; controls are labeled and correct in both light and dark themes.
  - Purpose: Confirm the feature meets all requirements and introduces no regressions.
  - _Leverage: composer test/stan/cs gate; templates dive_detail, divesite_detail, divegallery, divestats_
  - _Requirements: 1, 2, 3, 4, 5_
  - _Prompt: Implement the task for spec dive-photo-lightbox-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Release/QA Engineer | Task: Run the full quality gate `composer test && composer stan && composer cs` and perform manual browser E2E validation of requirements 1-5 across the dive detail, dive-site, gallery, and stats/certification pages, confirming grouped navigation, wrap-around, keyboard controls, the position indicator, single-image degradation, and correct theming | Restrictions: Do not modify production code as part of verification except to fix defects surfaced by the checks; if fixes are needed, keep them minimal and within the approved design | Success: The composer gate passes clean; every acceptance criterion is confirmed in the browser; grouping never mixes unrelated images; no console errors in any consuming page. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._
