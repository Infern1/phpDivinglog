# Tasks Document

- [x] 1. Extract the dive detail body into a shared content partial
  - Files: templates/partials/dive_detail_content.html.twig (new), templates/dive_detail.html.twig (modify)
  - Move the hero + `.dive-layout-grid` (sidebar + `.dive-content-column`) markup from dive_detail.html.twig's content block into a new `partials/dive_detail_content.html.twig`, and have dive_detail.html.twig include it (keeping the `{% extends 'layout.html.twig' %}` shell and the existing `<script>` tags).
  - Purpose: Enable the same detail markup to be rendered inside the full page shell and as a bare AJAX fragment with no duplication.
  - _Leverage: existing dive_detail.html.twig markup and DiveController::detail() view-model_
  - _Requirements: 4.4, Non-Functional Modular Design_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Twig Template Developer | Task: Extract the dive detail body (hero and the dive-layout-grid containing the logbook sidebar and dive-content-column) from templates/dive_detail.html.twig into a new templates/partials/dive_detail_content.html.twig, then include that partial from dive_detail.html.twig so the full page renders identically, per requirement 4.4 | Restrictions: Do not change any markup, class names, data attributes, or view-model keys; keep the script includes in the full page template only; keep output auto-escaped | Success: /dives/{n} renders byte-for-byte equivalent content and the existing dive detail smoke test still passes. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 2. Add a fragment root wrapper with client-contract data attributes to the content partial
  - File: templates/partials/dive_detail_content.html.twig (modify)
  - Wrap the partial's output in a single root element carrying `data-dive-fragment`, `data-dive-number="{{ dive.number }}"`, and `data-dive-title` (the same value used for the page title, e.g. site name or `Dive #{number}`); ensure the hero keeps class `dive-hero` and the main column keeps class `dive-content-column` so the client can target them.
  - Purpose: Give the client a stable contract to read the dive number/title and locate swap regions.
  - _Leverage: related_site_name / dive.number already available in the view-model_
  - _Requirements: 3.4, 4.4_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Twig Template Developer | Task: Wrap the shared content partial in a root element exposing data-dive-fragment, data-dive-number, and data-dive-title per requirements 3.4 and 4.4, computing the title as the related site name when present otherwise `Dive #{number}`, while keeping the hero (.dive-hero) and main column (.dive-content-column) selectors intact | Restrictions: Do not alter the sidebar structure or other classes; keep everything auto-escaped; the wrapper must not break the full-page layout/styling | Success: The full page still renders correctly and the fragment root exposes the number and title attributes. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 3. Create the partial page template
  - File: templates/dive_detail_partial.html.twig (new)
  - Render only `{% include 'partials/dive_detail_content.html.twig' %}` (no layout extend, no shell, nav, or footer).
  - Purpose: Provide the bare fragment the client swaps in.
  - _Leverage: templates/partials/dive_detail_content.html.twig_
  - _Requirements: 4.1, 4.2_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Twig Template Developer | Task: Create templates/dive_detail_partial.html.twig that includes only the shared content partial with no layout shell per requirements 4.1 and 4.2 | Restrictions: Must not extend layout.html.twig; must not include header/nav/footer or script tags; keep output auto-escaped | Success: Rendering this template yields just the detail fragment with the fragment root and no page chrome. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 4. Add the partial-vs-full dispatch branch in the front controller
  - File: public/index.php (modify the dives.detail branch)
  - Detect a partial request (header `X-Requested-With: XMLHttpRequest`, or `?partial=1` fallback); when partial, render `dive_detail_partial.html.twig`; otherwise render `dive_detail.html.twig` as today. Keep the shared 404 handling for a missing dive in both modes.
  - Purpose: Serve the fragment for AJAX while preserving full-page behavior.
  - _Leverage: existing dives.detail dispatch, DiveController::detail(), TwigRenderer, existing 404 branch_
  - _Requirements: 4.1, 4.2, 4.3, 3.3_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: PHP Developer working on front-controller dispatch | Task: In public/index.php, extend the dives.detail branch to detect a partial request via the X-Requested-With header (with a ?partial=1 fallback) and render dive_detail_partial.html.twig for partials and dive_detail.html.twig otherwise, reusing the existing DiveController::detail() call and 404 handling, per requirements 4.1-4.3 and 3.3 | Restrictions: Do not change the Router or the route key; do not duplicate the detail() call unnecessarily; keep the not-found response identical for both modes; keep dispatch style consistent | Success: GET /dives/1 with the header returns only the fragment, without the header returns the full page, and a missing dive returns 404 in both modes. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 5. Refactor the profile chart into an idempotent init entry point
  - File: public/assets/js/profile-chart.js (modify)
  - Replace the fire-once IIFE body with `window.DivelogProfileChart = { init(root = document) { ... } }` that finds `#profile-chart` (with its `data-dive-number`) within `root`, fetches and renders the chart, and wires `themechange`; auto-run `init()` once on DOMContentLoaded for the initial page. Guard against duplicate `themechange` bindings across repeated inits.
  - Purpose: Allow the chart to be (re)initialized after each in-place dive swap.
  - _Leverage: existing chart-drawing code and DivlogChartTheme (chart-theme.js)_
  - _Requirements: 5.1, 5.3_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Front-end Developer specializing in dependency-free vanilla JavaScript | Task: Refactor public/assets/js/profile-chart.js to expose window.DivelogProfileChart.init(root) that locates and renders the profile chart within root and is safe to call repeatedly, auto-initializing once on DOMContentLoaded, per requirements 5.1 and 5.3 | Restrictions: Preserve all existing drawing/interaction behavior and theme-awareness; do not leak duplicate themechange listeners across inits; no third-party libraries | Success: The chart still renders on initial load and can be re-initialized against freshly swapped content without duplicate listeners or console errors. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 6. Create the client navigation module for in-place dive swapping
  - File: public/assets/js/dive-detail-nav.js (new)
  - Delegate `click` on the document for `[data-logbook-link]` and `.dive-sequence-nav a[href]`; for same-origin `/dives/{number}` targets (ignoring modified/middle clicks), prevent default, fetch the URL with `X-Requested-With: XMLHttpRequest`, parse the fragment, replace `.dive-hero` and `.dive-content-column`, move the sidebar `.is-active`/`aria-current` to the selected item, update `document.title` and `history.pushState` to the dive URL, then call `window.DivelogProfileChart.init()` on the new content. Preserve window and sidebar scroll (no reset). Handle `popstate` by swapping to the URL. On any fetch/parse error, fall back to `window.location.assign(url)`.
  - Purpose: Deliver seamless, scroll-preserving dive navigation with correct history/title.
  - _Leverage: fragment data attributes from task 2, DivelogProfileChart.init from task 5, document-delegated lightbox_
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2, 5.2, 5.4_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Front-end Developer specializing in dependency-free progressive enhancement | Task: Create public/assets/js/dive-detail-nav.js implementing requirements 1, 2, 3, and 5 — delegate clicks on logbook links and prev/next arrows, fetch the /dives/{n} fragment via X-Requested-With, swap .dive-hero and .dive-content-column, update the sidebar active item, document.title, and history state, re-init the profile chart, preserve window and sidebar scroll, handle popstate, and fall back to a full-page navigation on any error | Restrictions: No third-party libraries; use event delegation so handlers survive swaps; do not replace or recenter the sidebar (preserve its scroll); ignore modified/middle/download/external clicks; guard all DOM access | Success: Clicking sidebar entries and arrows swaps content in place with no scroll jump, URL/title/active-state update, back/forward works, the chart and lightbox work on the new dive, and errors fall back to full navigation. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 7. Wire the navigation module into the full page and remove the superseded scroll workaround
  - Files: templates/dive_detail.html.twig (modify), public/assets/js/tables.js (modify)
  - Add `dive-detail-nav.js` as a deferred `script` include in dive_detail.html.twig (alongside the existing scripts). Remove the window-scroll-restore block added for detail navigation in tables.js (the `[data-detail-layout]` window-scroll/nav-flag logic), keeping the sidebar's own scroll-persistence logic intact.
  - Purpose: Activate seamless navigation and drop the now-redundant reload workaround.
  - _Leverage: existing script include pattern; tables.js logbook block_
  - _Requirements: 1.4, Non-Functional Reliability_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Front-end Developer | Task: Include dive-detail-nav.js in dive_detail.html.twig and remove the superseded [data-detail-layout] window-scroll-restore block from public/assets/js/tables.js per requirement 1.4, leaving the sidebar scroll-persistence logic untouched | Restrictions: Do not remove or alter the logbook sidebar scroll persistence; keep other tables.js behavior intact; place the new script with the other defer scripts | Success: The detail page loads the nav module, the old reload-scroll workaround is gone, and the sidebar scroll persistence still works. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 8. Extend the HTTP smoke test helper to support request headers
  - File: tests/Http/WebSmokeTest.php (modify the request() helper)
  - Add an optional headers parameter to `request()` that populates the corresponding `$_SERVER` keys (e.g. `HTTP_X_REQUESTED_WITH`) before including the front controller, without changing existing call sites.
  - Purpose: Allow tests to exercise the partial (AJAX) code path.
  - _Leverage: existing request() helper and fixture bootstrapping_
  - _Requirements: 4.1, Non-Functional Reliability_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: QA Engineer with PHPUnit HTTP testing expertise | Task: Extend the request() helper in tests/Http/WebSmokeTest.php to accept optional request headers and map them into $_SERVER (e.g. X-Requested-With to HTTP_X_REQUESTED_WITH) before including public/index.php, per requirement 4.1 | Restrictions: Keep the existing helper signature backward compatible (headers optional, default none); reset $_SERVER header keys between requests so tests stay isolated | Success: Existing tests still pass and a header can be supplied to drive the partial path. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 9. Add HTTP smoke tests for partial rendering, full rendering, and not-found
  - File: tests/Http/WebSmokeTest.php (extend)
  - Add: `testDiveDetailPartialReturnsFragmentOnly` (GET /dives/1 with X-Requested-With returns 200, contains the fragment root data-dive-number and .dive-content-column, and does NOT contain the shell: no `!doctype`, no primary nav, no footer); `testDiveDetailFullIncludesNavScript` (GET /dives/1 without header returns the full page and includes /assets/js/dive-detail-nav.js); `testDiveDetailPartialUnknownReturnsNotFound` (partial request for a missing dive returns 404).
  - Purpose: Lock the route contract, the fragment/shell boundary, and 404 behavior.
  - _Leverage: extended request() helper (task 8), sqlite fixtures_
  - _Requirements: 1.4, 3.3, 4.1, 4.2, 4.3_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: QA Engineer with PHPUnit HTTP smoke testing expertise | Task: Add smoke tests to tests/Http/WebSmokeTest.php covering the partial fragment response (fragment root and content column present, page shell absent), the full page including the nav script, and partial 404 for a missing dive, per requirements 1.4, 3.3, and 4.1-4.3, using the extended request() helper and fixtures | Restrictions: Reuse the existing helper and fixtures; keep tests isolated; assert both presence (fragment markers) and absence (shell markers) for the partial | Success: New tests pass under composer test and fail if the partial/full boundary or 404 handling regresses. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._

- [x] 10. Verify the full gate and manually validate acceptance criteria
  - Files: none (verification task)
  - Run `composer test && composer stan && composer cs`. Manually verify: sidebar and prev/next swap in place with no scroll jump (window + sidebar scroll preserved); active item, URL, and title update; browser back/forward restores the right dive; refresh and direct deep-link render the full page; profile chart renders per dive and respects light/dark theme; pictures lightbox (grouped prev/next) works on swapped dives; with JS disabled navigation still works as full loads; a simulated fetch failure falls back to full navigation.
  - Purpose: Confirm the feature meets all requirements with no regressions.
  - _Leverage: composer gate; dive_detail templates; profile-chart.js; lightbox.js_
  - _Requirements: 1, 2, 3, 4, 5_
  - _Prompt: Implement the task for spec dive-detail-ajax-navigation, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Release/QA Engineer | Task: Run `composer test && composer stan && composer cs` and perform manual browser E2E validation of requirements 1-5 for in-place dive navigation — seamless swap with preserved scroll, updated active state/URL/title, working back/forward, correct full-page refresh/deep-link, theme-aware chart per dive, working lightbox, JS-off fallback, and graceful full-navigation fallback on fetch error | Restrictions: Only modify production code to fix defects surfaced by the checks, staying within the approved design | Success: The gate passes clean and every acceptance criterion is confirmed with no console errors. Set the task to [-] in tasks.md before starting, log the implementation with log-implementation after completion, then mark it [x]._
